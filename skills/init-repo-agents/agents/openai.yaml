interface:
  display_name: "Init Repo Agents"
  short_description: "Create or refresh human-owned repository agent rules and docs contracts"
  default_prompt: "Use $init-repo-agents to establish one authoritative AGENTS.md, a CLAUDE.md import, concise area-based docs, and explicit Skill routing. Preserve custom rules and verify observable behavior."
