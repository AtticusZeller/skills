interface:
  display_name: "Init Repo Agents"
  short_description: "Bootstrap a repo's AGENTS.md, session lifecycle, and in-repo docs memory"
  default_prompt: "Use $init-repo-agents to collect project inputs, preview and run its bundled init-repo-agents.sh, then require check-repo-agents.sh to pass. Never recreate or summarize the template manually."
